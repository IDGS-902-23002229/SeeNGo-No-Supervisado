# Entorno / dependencias
.venv/
__pycache__/
*.pyc
# npm (solo se commitea lo vendorizado en pantalla/vendor/, no el árbol)
node_modules/

# Secretos (nunca subir)
.env

# Salidas generadas por el consumidor (se regeneran con un run)
/resultados.json
/pantalla/resultados.js

# Editor
.vscode/*.log
